# Checker 実装ロードマップ（履歴）

最終更新日: 2026-02-15
状態: 現在計画へ統合済み（履歴化）

## 概要

- 未実装 checker 14 game の段階的実装ロードマップとして作成。
- 実装順は `copl/` 番号昇順を基本とし、game ごとに TDD で進める方針を採用。
- ルール検証品質向上の改善項目（診断改善、異常系 fixture 拡張、CLI 分離検討）を管理対象に含めた。

## 取り扱い

- 現在の優先順位付き着手順は `docs/PLAN.md` の統合バックログを参照する。
- このファイルは移行前ロードマップの履歴として保持する。
